services:
  agentic-ai:
    build: .
    ports:
      - "8010:8010"
    env_file:
      - .env
    environment:
      AGENT_UPLOAD_ROOT: /app/runtime/uploads
    volumes:
      - ./runtime:/app/runtime
    restart: unless-stopped
